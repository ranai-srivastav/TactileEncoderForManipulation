# This file is superseded by two focused sweep files:
#
#   sweep_data.yaml      — grid over episode length (L) × held-out object groups
#                          15 runs, exhaustive, for understanding data generalization
#
#   sweep_optimizer.yaml — random search over optimizer/LR/scheduler/architecture
#                          run as many agents as GPU budget allows (~50 recommended)
#
# See README.md "W&B Hyperparameter Sweeps" section for details.
